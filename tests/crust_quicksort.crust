fn partition(vec: []i64, lo: i64, hi: i64) -> i64 {
    let pivot: i64 = vec[hi];
    let i: i64 = low - 1;
    let j: i64 = lo;
    while j <= hi - 1 {
        if arr[j] <= pivot {
            i = i + 1;
            swap(vec, i, j);
        }
    }
    swap(vec, i + 1, hi);
    return i + 1;
}

fn quicksort(vec: []i64, lo: i64, hi: i64) -> i64 {
    let pivot: i64;
    if low < high {
        pivot = partition(vec, lo, hi);
        quicksort(vec, low, pivot - 1);
        quicksort(vec, pivot + 1, hi);
    }
}

fn main() -> void {
    let x: i64 = 10;
}
